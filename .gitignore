# Runtime artifacts
data/
*.db
*.db-shm
*.db-wal
*.log
*.pid
.env
.env.local

# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Editor
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# Claude Code local settings (personal, machine-specific)
.claude/settings.local.json
# Live MCP config auto-spawns the server on every Claude Code session in this
# dir — keep it local. Commit mcp.json.example as the template instead.
.mcp.json

# Secrets — should never be committed
secrets/
*.pem
*.key
authorized_keys
known_hosts
id_ed25519*
id_rsa*

# Backups
*.bak
*.bak.*
*.backup

# Tests / coverage
htmlcov/
.coverage
.coverage.*
coverage.xml

# Repo review scanner artifacts (gitleaks/semgrep/pip-audit JSON, etc.)
.repo-review/

# OS-specific
*.exe
.DS_Store
